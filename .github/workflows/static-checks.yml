name: static-checks

on:
  push:
    branches:
      - feat/v1-portable-tray
  pull_request:
    branches:
      - main

jobs:
  windows-powershell-51:
    runs-on: windows-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run PowerShell 5.1 static tests
        shell: powershell
        run: |
          $PSVersionTable.PSVersion
          & .\tests\StaticTests.ps1

      - name: Run read-only power policy tests
        shell: powershell
        run: |
          & .\tests\PowerPolicyTests.ps1

      - name: Run policy transaction tests
        shell: powershell
        run: |
          & .\tests\PolicyTransactionTests.ps1

      - name: Run runtime policy lifecycle tests
        shell: powershell
        run: |
          & .\tests\RuntimePolicyTests.ps1

      - name: Run hosted Windows non-mutating E2E
        shell: powershell
        run: |
          & .\tests\WindowsIntegrationTests.ps1
